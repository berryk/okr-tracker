# OKR Tracker - Environment Configuration
# Copy to .env.homelab and customize

# ============ REQUIRED ============

# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here
SESSION_SECRET=your-session-secret-here

# ============ AI / LLM ============

# Provider: anthropic | openai | ollama | mock
LLM_PROVIDER=anthropic

# Anthropic (Claude) - Recommended
ANTHROPIC_API_KEY=sk-ant-your-key-here

# OpenAI (Alternative)
# OPENAI_API_KEY=sk-your-key-here

# Ollama (Local LLM)
# OLLAMA_HOST=http://host.docker.internal:11434
# OLLAMA_MODEL=llama3.1:8b

# ============ EMAIL ============

# Provider: console | smtp | sendgrid
EMAIL_PROVIDER=console

# SMTP settings (if EMAIL_PROVIDER=smtp)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@yourdomain.com

# SendGrid (if EMAIL_PROVIDER=sendgrid)
# SENDGRID_API_KEY=SG.xxx

# ============ DATABASE ============

# Usually no need to change for homelab
# DATABASE_URL=postgresql://okr:okr_secure_password@db:5432/okr_tracker
# REDIS_URL=redis://redis:6379

# ============ AUTH ============

# Mode: demo | okta
AUTH_MODE=demo

# Okta (if AUTH_MODE=okta)
# OKTA_ISSUER=https://your-org.okta.com/oauth2/default
# OKTA_CLIENT_ID=your-client-id
# OKTA_CLIENT_SECRET=your-client-secret

# ============ OPTIONAL ============

# Enable debug logging
# DEBUG=true

# Timezone
TZ=Europe/London
